#ifndef __NUMBER__
#define __NUMBER__

#include "linkedlist.h"
#include <string.h>

LinkedList *sum(LinkedList *, LinkedList *);
unsigned short getRest(unsigned short, unsigned short, unsigned short **);
char *listToString(LinkedList *);

#endif